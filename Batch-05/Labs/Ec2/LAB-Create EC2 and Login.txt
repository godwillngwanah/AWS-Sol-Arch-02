LAB - EC2	(Elastic Compute Cloud)

Sign in into your aws account and perform below tasks:


Create a windows and linix EC2 instance in the Amazon EC2 console and login using Mobaxterm;
   
    - Use the public IP address to remote login into both servers.

		a.	For Linux Server, use SSH protocol
		b.	For Windows Server, use RDP protocol	
